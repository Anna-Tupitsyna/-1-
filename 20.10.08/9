#include <iostream>
#include <algorithm>
#include <vector>
#include "stdio.h"
#include "math.h"
using namespace std;
 int main() {
    int a, b=0;
    cin >> a;
    int k=sqrt(a)+1;
    int*c=new int[a];
    for (int i=0; i<a; i++) {
        c[i]=i;
    }
    for (int i=0; i<a; i++) {
        for (int j=2; j<c[i]; j++) {
            if (c[i]%j==0) {
              c[i]=0;
            }
        }
    }
    for (int i=0; i<a; i++) {
        if (c[i]>1) {
            b=b+1;
            cout << c[i] << " ";
        }
    } 
    return 0;
}
Оценка сложности:
Двойной "индексный пробег" => О(n^2) + вывод
